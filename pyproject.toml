[project]
name = "cotton-filter"
version = "0.1.0"
description = "Excel cotton resource filtering GUI"
requires-python = ">=3.10,<3.11"
dependencies = [
    "openpyxl",
    "pandas",
    "pillow",
    "pystray",
    "xlrd",
]

[tool.uv]
package = false
